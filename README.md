# 副業潛力分析表

一個互動式靜態頁面，用來比較 6 大副業類型：

- 線上服務
- 內容創作
- 電商銷售
- 專業接案
- 勞務服務
- 數位產品

頁面會列出各類型的初期投入成本（時間／資金）、營收模式、回報周期與未來擴展性，並根據使用者輸入的收入目標、每週可投入時間、啟動資金、回報速度偏好與既有優勢，即時計算最適配的選擇建議。

## 使用方式

直接開啟 `index.html`，或用任一靜態伺服器預覽：

```bash
python3 -m http.server 8000
```

然後前往 `http://localhost:8000`。
